# Every shell command JetGrid local mode can execute

> Generated by `php artisan jetgrid:local-commands --markdown` from
> `App\Services\Local\LocalCommandRegistry`. Do not edit by hand.
>
> Generated: 2026-09-08 21:30:27

This is the complete list. `LocalCommandRunner` refuses any key that is not
here, every `{placeholder}` is validated against an exact pattern before the
command is built, and arguments reach the process as an **array** — no shell,
no string command line, so no argument value can introduce a second command.

## Rules that apply to every command on this page

- **Never run with sudo or as root.** Every entry has `needsRoot = false`, so
  none of them appears in `artifacts/jetgrid-sudoers`.
- **Never run unless the L0 gate passes.** All four checks — `jetgrid.mode`,
  `APP_ENV`, the absence of a production marker file, and
  `PlatformDetector::isLocalWorkstation()` — must pass, on reads as well as
  writes.
- **Never run inside a project directory in a way that writes to it.** A
  project directory is used as a working directory and read from. The only
  files local mode creates are under `storage/app/jetgrid/`.
- **Write commands are audited.** An `audit_logs` row is opened before the
  process starts and closed with its exit code, output and duration. Read
  commands are not audited: the run-state probe issues several per project
  every few seconds while a dashboard is open, and recording those would bury
  the records that matter.
- **A missing binary is not an error.** It degrades the layer that needed it.
  `php artisan jetgrid:doctor` reports which tools are present and what stops
  working without each one.

## All platforms (20)

### `docker.compose.ls`

```
docker compose ls --format json
```

- **Changes something:** no — read-only
- **Why:** List running compose projects, to match a container back to a discovered folder by compose project name.

### `docker.ps`

```
docker ps --format json
```

- **Changes something:** no — read-only
- **Why:** List running containers with their labels, including com.docker.compose.project.working_dir.

### `docker.compose.up`

```
docker compose up -d
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Start a project that ships a compose file. Run with the project directory as the working directory, so it needs no path argument.

### `docker.compose.down`

```
docker compose down
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Stop a compose project. The counterpart of the above, and the only stop path for a Docker-run project.

### `git.branch`

```
git --no-optional-locks rev-parse --abbrev-ref HEAD
```

- **Changes something:** no — read-only
- **Why:** Current branch for the detail panel. Read-only plumbing command.

### `git.dirty`

```
git --no-optional-locks status --porcelain
```

- **Changes something:** no — read-only
- **Why:** Whether the working tree is dirty. --porcelain because the human format is localised and changes between git versions; --no-optional-locks so reading a project never writes to its .git directory.

### `start.laravel`

```
php artisan serve --host=127.0.0.1 --port={port}
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Laravel dev server. --host is passed explicitly rather than trusting artisan defaults, so a discovered project is never exposed to the LAN.
- **Argument patterns:**
  - `port` — `/^(?:[1-9][0-9]{0,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5])$/`

### `start.php_builtin`

```
php -S 127.0.0.1:{port}
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** The PHP built-in server, for static sites and PHP projects with no framework runner.
- **Argument patterns:**
  - `port` — `/^(?:[1-9][0-9]{0,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5])$/`

### `start.node`

```
npm run {script}
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Node dev server via npm. The script name comes from the project package.json and is pattern-checked before it is used.
- **Argument patterns:**
  - `script` — `/^[a-zA-Z0-9][a-zA-Z0-9:_-]{0,40}$/`

### `start.node.yarn`

```
yarn run {script}
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Same, for a project with a yarn.lock. Using the wrong package manager is the most common reason a dev server will not boot.
- **Argument patterns:**
  - `script` — `/^[a-zA-Z0-9][a-zA-Z0-9:_-]{0,40}$/`

### `start.node.pnpm`

```
pnpm run {script}
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Same, for a project with a pnpm-lock.yaml.
- **Argument patterns:**
  - `script` — `/^[a-zA-Z0-9][a-zA-Z0-9:_-]{0,40}$/`

### `start.django`

```
python manage.py runserver 127.0.0.1:{port}
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Django dev server, bound to loopback.
- **Argument patterns:**
  - `port` — `/^(?:[1-9][0-9]{0,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5])$/`

### `start.rails`

```
bin/rails server -p {port}
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Rails server via the project binstub, which is what picks up its bundle.
- **Argument patterns:**
  - `port` — `/^(?:[1-9][0-9]{0,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5])$/`

### `start.go`

```
go run .
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Go program in the project root. Go has no port convention, so the resolved port is informational for this type.

### `install.composer`

```
composer install
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Install PHP dependencies. A one-click action behind a confirmation showing the literal command - never part of a start.

### `install.npm`

```
npm install
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Install Node dependencies. Same confirmation rule.

### `install.yarn`

```
yarn install
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Install Node dependencies for a yarn project.

### `install.pnpm`

```
pnpm install
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Install Node dependencies for a pnpm project.

### `laravel.migrate`

```
php artisan migrate
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Run a discovered Laravel project own migrations. The most destructive action in this registry, and the reason the confirmation echoes the literal command.

### `editor.open`

```
code {path}
```

- **Changes something:** no — read-only
- **Why:** Open a project in VS Code. An absent CLI is not an error - the action is simply not offered.
- **Argument patterns:**
  - `path` — `#^(?:[A-Za-z]:[\\/]|\\\\[^\\/:*?"<>|\r\n]{1,63}[\\/]|/)[^\r\n\x00*?"<>|]{0,4000}$#`

## Linux (5)

### `linux.port.owner`

```
ss -tlnp
```

- **Changes something:** no — read-only
- **Why:** Listening sockets with owning PIDs. Reads the whole table once, for the same reason as netstat.

### `linux.port.owner.lsof`

```
lsof -nP -iTCP:{port} -sTCP:LISTEN
```

- **Changes something:** no — read-only
- **Why:** Fallback for hosts with no iproute2. Registered separately so the doctor can report which of the two is actually present.
- **Argument patterns:**
  - `port` — `/^(?:[1-9][0-9]{0,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5])$/`

### `linux.process.cwd`

```
readlink /proc/{pid}/cwd
```

- **Changes something:** no — read-only
- **Why:** Working directory of an attributed PID, straight from procfs. Fails soft when the process belongs to another user.
- **Argument patterns:**
  - `pid` — `/^[1-9][0-9]{0,9}$/`

### `linux.process.name`

```
ps -o comm= -p {pid}
```

- **Changes something:** no — read-only
- **Why:** Executable name for an attributed PID.
- **Argument patterns:**
  - `pid` — `/^[1-9][0-9]{0,9}$/`

### `linux.process.commandline`

```
ps -o args= -p {pid}
```

- **Changes something:** no — read-only
- **Why:** Full command line, used to match a project path when procfs is unreadable.
- **Argument patterns:**
  - `pid` — `/^[1-9][0-9]{0,9}$/`

## Windows (4)

### `windows.port.owner`

```
netstat -ano -p TCP
```

- **Changes something:** no — read-only
- **Why:** Map a listening TCP port to a PID. netstat has no port filter, so the whole table is read once and filtered in PHP - cheaper than one process per port.

### `windows.process.name`

```
tasklist /FI PID eq {pid} /NH /FO CSV
```

- **Changes something:** no — read-only
- **Why:** Image name for an attributed PID, so the panel can say "php.exe" rather than a bare number.
- **Argument patterns:**
  - `pid` — `/^[1-9][0-9]{0,9}$/`

### `windows.process.commandline`

```
powershell -NoProfile -NonInteractive -Command Get-CimInstance Win32_Process -Filter "ProcessId = {pid}" | Select-Object -ExpandProperty CommandLine
```

- **Changes something:** no — read-only
- **Why:** Read a PID command line to confirm which project owns a port. The only interpolation is a PID constrained to digits, so the -Command script cannot be extended by an argument value.
- **Argument patterns:**
  - `pid` — `/^[1-9][0-9]{0,9}$/`

### `windows.process.kill`

```
taskkill /PID {pid} /T /F
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** Stop a dev server and its children. /T because npm and artisan both spawn a child that would otherwise keep holding the port.
- **Argument patterns:**
  - `pid` — `/^[1-9][0-9]{0,9}$/`

## macOS (4)

### `macos.port.owner`

```
lsof -nP -iTCP:{port} -sTCP:LISTEN
```

- **Changes something:** no — read-only
- **Why:** PID listening on a port. -n and -P skip DNS and service-name lookups, which is the difference between 20ms and a second.
- **Argument patterns:**
  - `port` — `/^(?:[1-9][0-9]{0,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5])$/`

### `macos.process.cwd`

```
lsof -a -p {pid} -d cwd -Fn
```

- **Changes something:** no — read-only
- **Why:** Working directory of an attributed PID. -Fn gives machine-readable output rather than a table whose shape depends on terminal width.
- **Argument patterns:**
  - `pid` — `/^[1-9][0-9]{0,9}$/`

### `macos.process.name`

```
ps -o comm= -p {pid}
```

- **Changes something:** no — read-only
- **Why:** Executable name for an attributed PID.
- **Argument patterns:**
  - `pid` — `/^[1-9][0-9]{0,9}$/`

### `macos.process.commandline`

```
ps -o command= -p {pid}
```

- **Changes something:** no — read-only
- **Why:** Full command line, used when lsof cannot read the working directory and the path has to be matched out of the arguments instead.
- **Argument patterns:**
  - `pid` — `/^[1-9][0-9]{0,9}$/`

## macOS and Linux (2)

### `unix.process.term`

```
kill -15 {pid}
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** SIGTERM, giving a dev server the chance to release its port cleanly. Used only when ext-posix is unavailable - posix_kill needs no process at all.
- **Argument patterns:**
  - `pid` — `/^[1-9][0-9]{0,9}$/`

### `unix.process.kill`

```
kill -9 {pid}
```

- **Changes something:** yes — audited, and refused unless the gate passes
- **Why:** SIGKILL, after SIGTERM has been given time to work.
- **Argument patterns:**
  - `pid` — `/^[1-9][0-9]{0,9}$/`

## Commands local mode will never run

Listed because their absence is a design decision, not an oversight:

- `composer install`, `npm install` and `php artisan migrate` exist in the
  registry but are **never run automatically**. `ProcessController::maintenance()`
  refuses them unless the caller echoes back the exact rendered command line.
- No command writes into a discovered project directory.
- No command is passed to a shell. `powershell -Command` is the one entry that
  interpolates a value into a script, and that value is a PID constrained to
  `/^[1-9][0-9]{0,9}$/`.
- Detached dev servers are spawned with `proc_open` and an argv array, not
  Symfony Process: Process kills its child in its destructor, which would stop
  every dev server the moment the request that started it ended.
