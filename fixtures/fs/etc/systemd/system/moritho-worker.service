[Unit]
Description=Moritho queue worker
After=network.target

[Service]
User=moritho
ExecStart=/usr/bin/php /var/www/moritho/current/artisan queue:work --sleep=3 --tries=3
Restart=always

[Install]
WantedBy=multi-user.target
